#include <stdio.h>

/**
 * print_chessboard - Function that prints the chessboard.
 *
 * @a: input pointer
 *
 * Return: nothing
*/

void print_chessboard(char (*a)[8])
{
	int h, d;

	for (h = 0; h < 8; h++)
	{
		for (d = 0; d < 8 ; d++)
			printf("%c", a[h][d]);

		printf("\n");
	}

}
