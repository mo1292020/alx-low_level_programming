#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * @argc: int parameter
 * @argv: string parameter
 * Return: Always 0 (Success)
*/

int main(int argc, char *argv[])
{
	int h;

	for (h = 0; h < argc; h++)
		printf("%s\n", argv[h]);

	return (0);
}
