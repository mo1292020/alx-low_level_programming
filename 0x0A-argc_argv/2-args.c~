#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * @argc: int parameter
 * @argv: string parameter
 * Return: Always 0 (Success)
*/

int main(int argc, char __attribute__((unused))*argv[])
{
	printf("%i\n", argc - 1);

	return (0);
}
